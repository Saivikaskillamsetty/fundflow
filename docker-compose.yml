services:
  postgres:
    image: postgres:16
    container_name: fundflow_pg
    restart: unless-stopped
    environment:
      POSTGRES_USER: fundflow
      POSTGRES_PASSWORD: fundflow
      POSTGRES_DB: fundflow
    ports:
      - "5433:5432"
    volumes:
      - fundflow_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fundflow"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  fundflow_pgdata:
